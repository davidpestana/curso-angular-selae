import { DirectivaClienteDirective } from './directiva-cliente.directive';

describe('DirectivaClienteDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaClienteDirective();
    expect(directive).toBeTruthy();
  });
});
